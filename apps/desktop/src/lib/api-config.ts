/** Local API used by Vite/Tauri development when no build-time URL is set. */
export const DEFAULT_API_BASE_URL = "http://localhost:3000";

/** Railway API bundled into the public Windows release. */
export const PRODUCTION_API_BASE_URL =
  "https://launcher-api-production-e506.up.railway.app";
